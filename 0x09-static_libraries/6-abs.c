#include "main.h"

/**
 * _abs _ a function that computes the absolute value of integer
 * @ab: integer input
 * Return:absolute value of ab
 */
int_abs(int ab)
{
 return (ab * ((ab > 0) - (ab < 0)));
}
